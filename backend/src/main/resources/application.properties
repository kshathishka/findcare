spring.application.name=backend
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Server Configuration
server.port=${PORT:${SERVER_PORT:8080}}
server.servlet.context-path=${SERVER_CONTEXT_PATH:/}

# JPA Configuration
spring.jpa.open-in-view=false

# JWT Configuration
app.jwt.secret=${JWT_SECRET:JWTSecretKeyForFindCareApplicationSecureAndStrong2024ExtraSecurePadding!!}
app.jwt.expiration=${JWT_EXPIRATION_MS:86400000}

# CORS Configuration
# Format: comma-separated list of allowed origins
# Examples: http://localhost:5173,https://app.example.com,https://findcare-frontend.vercel.app
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://findcare-frontend.vercel.app}

# Rate Limiting Configuration
app.auth.rate-limit.max-attempts=${AUTH_RATE_LIMIT_MAX_ATTEMPTS:5}
app.auth.rate-limit.window-seconds=${AUTH_RATE_LIMIT_WINDOW_SECONDS:60}

# Logging Configuration
logging.level.root=${LOG_LEVEL:INFO}
logging.level.com.findcare=${LOG_LEVEL_APP:DEBUG}
logging.pattern.console=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%X{requestId}] [%thread] %logger{36} - %msg%n

# Database Configuration - PostgreSQL on Render
spring.datasource.url=jdbc:postgresql://dpg-d72bepe3jp1c73ebpmog-a.onrender.com:5432/findcare_db
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=findcare_db_user
spring.datasource.password=TVSF6XYpHaxUU5yde1kfrhActLHKrxIL

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect